version: '3.9'

services:
  postgres:
    image: postgres:15-alpine
    container_name: banksystem-postgres
    restart: always
    environment:
      POSTGRES_DB: BankSystem
      POSTGRES_USER: vovgoo
      POSTGRES_PASSWORD: StrongestLocalPassword 
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vovgoo -d BankSystem"]
      interval: 5s
      timeout: 5s
      retries: 5

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: banksystem-server
    restart: always
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/BankSystem
      SPRING_DATASOURCE_USERNAME: vovgoo
      SPRING_DATASOURCE_PASSWORD: StrongestLocalPassword
      CORS_ALLOWED_ORIGINS: http://localhost:3000
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8080/actuator/health || exit 1"]
      interval: 30s
      timeout: 3s
      retries: 5

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: banksystem-client
    restart: always
    ports:
      - "3000:80"
    depends_on:
      server:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost/health || exit 1"]
      interval: 30s
      timeout: 3s
      retries: 5

volumes:
  postgres_data: